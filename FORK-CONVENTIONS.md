# FORK-CONVENTIONS.md — 二开规约（fork-local，与上游零冲突）

- **文档更新时间**：2026-08-18 03:50:49
- **文档版本**：V2
- **dsh 程序版本**：`0.1.0-rc.5`（commit `12720b0ef9`，分支 `wkj-dev`）

| 版本 | 更新时间 | dsh 版本 | 说明 |
| --- | --- | --- | --- |
| V1 | 2026-08-18 03:33:33 | `0.1.0-rc.5`（12720b0ef9） | 初始版本：自动提交机制、分支/目录/文档规约 |
| V2 | 2026-08-18 03:50:49 | `0.1.0-rc.5`（12720b0ef9） | 提交信息改用中文说明；明确自有文档根路径为 `my-docs/` |

本文件是本仓库二开（`wkj-dev` 分支）规约的**唯一事实来源**。上游同步不触碰本文件；`AGENTS.md` 仅保留一行指向本文件的索引。所有二开规则写在这里，不写进 `AGENTS.md`。

## 1. 阅读时机（会话开始）

每个会话开始、做任何仓库操作之前，先阅读本文件与 `AGENTS.md`。本文件头部有版本历史，修订过先看变更说明。

## 2. 分支与远程

- `master` 是纯净镜像，等于 `upstream/master`，**永不提交**。
- 一切二开落在 `wkj-dev`，推送 `gitea`（本地备份）+ `origin`（GitHub fork）。
- 同步流程：`git fetch upstream` → `master` 快进到 `upstream/master` → `merge master` 到 `wkj-dev`（本文件与 AGENTS.md 的 fork-local 内容在合并时保留）。

## 3. 自动提交机制（核心）

按工作实际进展自动提交，让 `wkj-dev` 始终可回溯、可备份，不攒到会话结束。

- **触发时机**（一个工作单元完成即提交）：
  - 新增或修改一个插件，且通过 `node my-plus/scripts/check.mjs` 校验；
  - 完成一份文档或一次文档修订；
  - 一次修复验证通过；
  - 约定或配置变更落地。
- **粒度**：一个逻辑单元一个提交；独立变更拆开提交；禁止把无关改动混进同一个提交。
- **提交前检查**：运行本次改动最窄的相关检查（插件 → `check.mjs`；文档 → 头部格式与链接；代码 → 对应测试）。提交前 `git diff --check`。
- **提交信息规范**（**说明用中文**）：
  - 格式：`<类型>(<范围>): <中文摘要>`；需要补充说明时在空行后写中文正文。
  - 类型用中文词：`新增`（feat）/ `修复`（fix）/ `文档`（docs）/ `杂项`（chore）/ `重构`（refactor）/ `测试`（test）。
  - 范围：`my-plus` / `my-docs` / `fork` / 插件 id 等。
  - 摘要用中文描述本次变更（标识符、路径等专名可保留原文）。
  - 示例：`新增(my-plus): 添加 session-orb 会话状态球插件`
- **推送**：提交后立即推送 `gitea` 与 `origin`，保持备份新鲜。
- **禁区**：不对共享分支 `--force`；不直接提交 `master`；沙箱拒绝文件操作时向用户申请升级，不绕过。

## 4. 二开产物目录规约

| 目录/文件 | 内容 | 校验 |
| --- | --- | --- |
| `my-plus/` | 自定义插件仓库：`plugins/<id>/manifest.json + host.js/client.js + README.md` | `node my-plus/scripts/check.mjs` |
| `my-docs/` | **自有文档唯一根路径**（仓库根 `my-docs/`）：所有自有文档统一存放于此，不散落其他位置；子目录用数字前缀分主题（如 `00-分析报告`、`21-Cordis插件相关`） | 头部格式 |
| `FORK-CONVENTIONS.md` + `AGENTS.md` 索引行 | 二开规约 | — |

不修改 `packages/`、`docs/`、`vendor/` 等上游文件，除非确有二开能力需求（先与上游同步评估，再以独立提交落地）。

## 5. 文档规范（my-docs）

- 头部元信息：文档更新时间（精确到秒）、文档版本 `V1..V{n}`、dsh 程序版本号。
- 版本历史表：每次修订追加一行并升版本号。

## 6. 本文件自身

本文件变更同样走自动提交；修订后更新头部时间与版本号。
